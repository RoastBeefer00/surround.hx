(define package-name 'surround.hx)
(define version "0.1.0")

(define dependencies
  '((#:name helix-vim-plugin #:git-url "https://github.com/RoastBeefer00/helix-vim-plugin.git")))

(define dylibs '())
